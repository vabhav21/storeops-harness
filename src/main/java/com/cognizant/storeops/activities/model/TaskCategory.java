package com.cognizant.storeops.activities.model;

/** Kind of retail store work an activity represents. PLANOGRAM is the category used by programme task templates. */
public enum TaskCategory {
    RESTOCKING,
    PLANOGRAM,
    AUDIT,
    COMPLIANCE,
    GENERAL
}
