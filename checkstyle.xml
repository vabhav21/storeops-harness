<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
        "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">

<!--
  StoreOps Checkstyle ruleset.

  This file is the machine-enforceable half of
  .harness/skills/coding-conventions/SKILL.md — every rule here exists so
  that a Generator convention violation fails `mvn checkstyle:check`
  (a Dimension 1 hard gate in .harness/skills/grading-criteria/SKILL.md)
  instead of relying on an LLM noticing it during review.

  Deliberately NOT enforced: Javadoc presence. The convention in this
  repository is "Javadoc where the reasoning is non-obvious (module
  boundary, event contract, layer role)", which a counting rule cannot
  express — that judgement stays with the Evaluator's how-to-review pass.
-->
<module name="Checker">
    <property name="severity" value="error"/>
    <property name="fileExtensions" value="java"/>

    <module name="FileTabCharacter"/>
    <module name="LineLength">
        <property name="max" value="140"/>
        <property name="ignorePattern" value="^package.*|^import.*|https?://"/>
    </module>

    <module name="TreeWalker">
        <!-- Imports: star imports hide cross-module dependencies from the
             boundary grep the Evaluator runs, so they are banned outright. -->
        <module name="AvoidStarImport"/>
        <module name="RedundantImport"/>
        <module name="UnusedImports"/>

        <!-- Naming: enforces the ubiquitous-language rules in
             coding-conventions (entities, enums, *Service/*ServiceImpl). -->
        <module name="PackageName"/>
        <module name="TypeName"/>
        <module name="MethodName"/>
        <module name="MemberName"/>
        <module name="ParameterName"/>
        <module name="LocalVariableName"/>
        <module name="ConstantName"/>

        <!-- Structure -->
        <module name="ModifierOrder"/>
        <module name="NeedBraces"/>
        <module name="LeftCurly"/>
        <module name="RightCurly"/>
        <module name="OneStatementPerLine"/>
        <module name="EmptyStatement"/>
        <module name="EqualsHashCode"/>
        <module name="MissingSwitchDefault"/>
        <module name="NoFinalizer"/>
        <module name="UpperEll"/>
        <module name="ArrayTypeStyle"/>
        <module name="WhitespaceAfter"/>
    </module>
</module>
